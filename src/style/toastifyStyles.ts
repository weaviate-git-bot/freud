import { globalCss } from '../stitches';

export const toastifyStyles = globalCss({
  '.Toastify--animate': {
    animationDuration: '0.15s',
  },

  '.Toastify--animate-icon': {
    animationDuration: '0.3s',
  },
});
